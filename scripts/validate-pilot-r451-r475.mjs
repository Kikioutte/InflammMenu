import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{validateCatalogue}from"./validate-catalogue.mjs";const root=new URL("../",import.meta.url),pilot=JSON.parse(await readFile(new URL("research/pilot-r451-r475.draft.json",root),"utf8")),concepts=JSON.parse(await readFile(new URL("research/recipes-r351-r500.json",root),"utf8")).filter(({id})=>+id.slice(1)>=451&&+id.slice(1)<=475),result=validateCatalogue(pilot, { taxonomy: "legacy" });assert.equal(result.schemaVersion,"2.1.0");assert.equal(result.recipeCount,25);assert.deepEqual(pilot.recipes.map(r=>r.id),concepts.map(c=>c.id));const byId=new Map(concepts.map(c=>[c.id,c]));for(const r of pilot.recipes){const c=byId.get(r.id);assert.equal(r.titre,c.titre);assert.equal(r.categorie,c.categorie);assert.deepEqual(r.saisons,c.saisons);assert.ok(r.ingredients.length>=4);assert.ok(r.etapes.length>=4);assert.equal(r.app.review.stage,"draft");assert.equal(r.app.review.status,"caution");assert.equal(r.app.planner.eligible,false);assert.equal(r.nutrition_par_portion.estimation.statut,"estimated");assert.equal(r.score_anti_inflammatoire,0);}assert.deepEqual(pilot.recipes.find(r=>r.id==="r474").app.planner.allergens.sort(),["fruits-a-coque","gluten","oeuf"]);assert.deepEqual(pilot.recipes.find(r=>r.id==="r475").app.planner.allergens.sort(),["fruits-a-coque","lait"]);console.log(`Lot r451-r475 v${result.schemaVersion} valide : ${result.recipeCount} recettes brouillon, toutes exclues du planificateur.`);
