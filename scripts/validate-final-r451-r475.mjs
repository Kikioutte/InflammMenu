import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{validateCatalogue}from"./validate-catalogue.mjs";const root=new URL("../",import.meta.url),[n,f,m,c]=await Promise.all(["research/pilot-r451-r475.nutrition.json","research/pilot-r451-r475.final.json","research/ciqual-map-r451-r475.json","research/ciqual-2025-core.json"].map(x=>readFile(new URL(x,root),"utf8").then(JSON.parse)));assert.equal(m.meta.ingredient_count,68);assert.equal(m.meta.reused_count,63);const codes=new Set(c.foods.map(x=>x.code));for(const x of m.ingredients)if((x.source_dataset??"ciqual")==="ciqual")assert.ok(codes.has(String(x.selected_ciqual_code)));assert.equal(validateCatalogue(f).recipeCount,25);assert.match(f.meta.culinary_notice,/aucune.*testée physiquement/i);const by=new Map(n.recipes.map(r=>[r.id,r]));for(const r of f.recipes){const s=by.get(r.id);for(const k of["titre","categorie","temps","portions","ingredients","etapes","nutrition_par_portion"])assert.deepEqual(r[k],s[k]);assert.equal(r.app.planner.eligible,false);assert.match(r.app.review.summary,/ne constitue pas seul un repas complet/i);assert.match(r.score_note,/ne mesure aucun effet médical/i);const a=[...new Set(r.ingredients.flatMap(i=>i.allergenes))].sort();assert.deepEqual([...r.app.planner.allergens].sort(),a);if(r.nutrition_par_portion.estimation.statut==="calculated-with-cautions")assert.match(r.app.review.caution,/réserve de calcul/i);}assert.match(f.recipes.find(r=>r.id==="r474").app.review.caution,/œuf à cœur/i);assert.match(f.recipes.find(r=>r.id==="r475").app.review.caution,/fromage au froid/i);console.log("Lot final r451-r475 valide : 25 accompagnements/collations relus, aucun faux repas activé.");
