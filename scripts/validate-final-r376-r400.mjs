import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{validateCatalogue}from"./validate-catalogue.mjs";const root=new URL("../",import.meta.url),[n,f,m,c]=await Promise.all(["research/pilot-r376-r400.nutrition.json","research/pilot-r376-r400.final.json","research/ciqual-map-r376-r400.json","research/ciqual-2025-core.json"].map(x=>readFile(new URL(x,root),"utf8").then(JSON.parse))),X=new Set(["r382","r391","r393","r396","r400"]);assert.equal(m.meta.ingredient_count,89);assert.equal(m.meta.reused_count,58);const codes=new Set(c.foods.map(x=>x.code));for(const x of m.ingredients)if((x.source_dataset??"ciqual")==="ciqual")assert.ok(codes.has(String(x.selected_ciqual_code)));assert.equal(validateCatalogue(f, { taxonomy: "legacy" }).recipeCount,25);assert.match(f.meta.culinary_notice,/aucune.*testée physiquement/i);const by=new Map(n.recipes.map(r=>[r.id,r]));for(const r of f.recipes){const s=by.get(r.id);for(const k of["titre","categorie","temps","portions","ingredients","etapes","nutrition_par_portion"])assert.deepEqual(r[k],s[k]);assert.equal(r.app.planner.eligible,!X.has(r.id));assert.match(r.score_note,/ne mesure aucun effet médical/i);assert.match(r.app.review.caution,/cuire à cœur/i);const a=[...new Set(r.ingredients.flatMap(i=>i.allergenes))].sort();assert.deepEqual([...r.app.planner.allergens].sort(),a);if(X.has(r.id)){assert.ok(r.nutrition_par_portion.sodium_mg>=800);assert.match(r.app.review.caution,/sodium calculé élevé/i);}if(r.nutrition_par_portion.estimation.statut==="calculated-with-cautions")assert.match(r.app.review.caution,/réserve de calcul/i);}assert.equal(f.recipes.filter(r=>r.app.planner.eligible).length,20);console.log("Lot final r376-r400 valide : 20 repas éligibles, 5 exclusions sodium.");
