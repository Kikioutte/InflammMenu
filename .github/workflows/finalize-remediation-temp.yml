name: Finalize audit remediation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary workflow paused while the verified payload is restored."
